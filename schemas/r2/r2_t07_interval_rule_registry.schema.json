{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "r2_t07_interval_rule_registry.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["task_id", "contract_version", "registry_type", "rules", "source_binding"],
  "properties": {
    "task_id": {"const": "R2-T07"},
    "contract_version": {"const": "r2_t02_confirmed_event_zone_state_machine_contract.v8"},
    "registry_type": {"const": "interval_rule_registry"},
    "rules": {"type": "object", "minProperties": 8},
    "source_binding": {"type": "object", "required": ["path", "git_blob_sha", "committed_byte_sha256"]}
  }
}
