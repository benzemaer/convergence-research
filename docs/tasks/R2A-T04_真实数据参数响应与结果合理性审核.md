# R2A-T04 真实数据参数响应与结果合理性审核

## 1. 定位与当前状态

本任务在 accepted canonical PCAVT Score release 上运行已接受的
`r2a_t03_dynamic_evaluator.v1`，审核预先冻结的 16-request representative panel 的真实参数响应、
状态区间结构、市场上下文与后续路径。当前仅完成 implementation candidate 与 synthetic
end-to-end smoke；尚未读取真实 Score/market database，也未消费唯一 formal attempt。

```text
task_id: R2A-T04
status: implementation_candidate
base_main_sha: a2c2ee0a7857fad86e4b8b14f6bf82f0d24a639a
formal_authorization_id: R2A-T04-REAL-AUDIT-AUTH-20260719
formal_run_authorized: false
formal_run_consumed: false
real_score_data_read: false
real_dynamic_evaluation_executed: false
owner_visual_review: not_started
R2A-T04_DONE: absent
R2A-T05_allowed_to_start: false
```

本任务不选择最佳参数，不注册 canonical dynamic-state version，不把 future path 指标解释成回测收益，
也不自动填写人工图形审核字段。

## 2. 不可变输入绑定

Score release 固定为 `pcavt-score-w120-v1-c7e04f11a2cd09aa`，数据库 SHA-256 为
`d1ee60ef854a5fe18042c61175febd837db43d76c5c104462ce61c3f176403a3`，字节数
`4255395840`。Accepted T01 handoff、T03 handoff/config、evaluator/output schema/protocol version
均在 config 中冻结。Score 与 market database 只能 read-only 打开；每次真实 preflight 和 formal
执行前后都重新验证 SHA-256 与大小。

Market context 是独立诊断源，只提供 raw/adjusted OHLC、volume、amount、可选 turnover 与交易状态；
不得进入 request identity 或改变 dynamic state。Local-only source spec 记录完整 SQL、列映射、单位、
snapshot、coverage 和数据库 identity，不提交绝对路径。

## 3. 16-request panel

Panel `r2a_t04_representative_panel.v1` 恰含 16 个唯一 canonical identities：五级 dimension ladder、
PCAVT equal-q 10/15/20/25、K=2/3/5/7，以及分别只放宽 P/C/A/V/T 到 q=2500 的五个 marginal
requests；D05 在比较组间复用，不重复建立。Panel 仅用于代表性响应审计，不构成候选最佳集合。

Chart audit 只覆盖 D03、D04、D05、Q02，用于比较加入 V、加入 T，以及完整 PCAVT 在 q=0.15/0.20
下的形态差异。

## 4. 强制 smoke 与并行边界

Formal full-universe request concurrency 永远为 1；16 个 request 严格串行，不启动 16 个 Python
workers，也不允许多个进程同时扫描 accepted Score database。单个 request 可使用冻结的 DuckDB
internal threads。Chart rendering 不与 evaluator 同时运行，worker count 不超过 4；首版固定为 1。

Synthetic end-to-end smoke 在 pytest temporary directory 内运行两个 canonical requests、至少三只证券，
同时覆盖 confirmed interval 和 zero-event，并贯通 evaluator、独立 output validator、request/year/
termination metrics、response checks、market rolling context、T+5/T+10/T+20、MFE/MAE、chart sampling、
PNG 与 smoke review schema；不提交 smoke 产物。

Implementation Quality 成功后、authorization commit 前，使用 D05 和由
`sha256(score_release_id + ':' + security_id)` 排序得到的固定四只证券，对 4/8/16 threads 分别运行
完整历史 benchmark。三组 validator/fingerprint 必须一致；选择最快值，若与较低线程配置差异小于
10%则选择较低值。选定 threads 与 benchmark fingerprint 写入 authorization metadata，不进入 request
hash，formal 中不得改变。

Authorization Quality 成功后，使用同四证券、同 D05、同 threads 执行 real-input smoke，实际验证
Score/market identity、accepted evaluator/validator、market reconciliation、metrics/path、可用时一张
chart 和 smoke review schema。四证券无 interval 是合法 zero-interval smoke，不得更换证券。Receipt
写入 local-only `preflight/real_input_smoke_receipt.json`，明确不消费 formal attempt。

## 5. Formal gate 与执行

首个 full-universe request 只有在 implementation/authorization Quality、synthetic smoke、thread
benchmark、real-input smoke、clean worktree、Score/market identity 和三倍 Score 大小的 free-disk gate
全部通过后才能开始。Formal 一旦开始即消费唯一 attempt；失败证据保留，禁止自动重跑、resume、换参数
或删除失败目录。

每个 request 使用 full 800-security scope 和完整 observation history。Accepted evaluator 与独立
validator 通过后，保存五表 row count/fingerprint、request/year/termination metrics、response key
extract、interval path 和 chart context，再删除大型 result DuckDB。四个 chart request 各按冻结 strata
选择 12 个 interval，共 48 PNG；人工 worksheet 字段保持空白。

## 6. 科学与结果边界

硬校验包括 q raw/confirmed subset、q joint-ready equality、K raw equality/confirmed contraction、
dimension ladder contraction、marginal target superset 与 non-target invariance，并要求响应非退化。
Market diagnostics 输出 MA cloud、price-to-cloud、ATR/NATR、range、volume/amount/turnover ratios；未来路径
按证券真实 observation offset 计算 T+5/10/20、MFE、MAE 和 peak/trough time。所有 future metrics 只做
描述，不是预测标签、信号或策略收益。

Formal 成功后只提交不超过 60 MB 的 compact review bundle，不提交 Score/MarketDB、完整 result DB、
audit database 或 interval parquet。最终 recommendation 只能继续 owner visual review 或阻塞；本任务不创建
`DONE`，不允许 R2A-T05。

## 7. 两阶段提交与停止点

Implementation commit `feat: implement R2A-T04 real-data audit harness` 必须先通过精确 HEAD Quality。
随后 benchmark 冻结 threads，authorization commit 只修改 config/docs metadata，parent 必须精确等于
implementation HEAD，且再次通过 Quality。Result commit 只提交 compact review bundle 和状态文档，PR
始终保持 Draft。

最终停止点：

```text
R2A-T04 real-result review
owner visual review pending
R2A-T04_DONE absent
R2A-T05_allowed_to_start false
```
