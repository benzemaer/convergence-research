# G0-T02：原始快照受控交付与独立哈希复核

> Task ID：G0-T02
> 状态：handoff_package_prepared_review_pending
> 关联任务：G0-T01
> 关联决策：DR-001
> 目标门禁：G0
> 当前运行资格：blocked

## 1. 背景

PR #3 已合并，但独立审核评论 `4875330585` 的结论为“部分通过，暂不批准 G0”。
阻塞项不是 tracked manifest 的形式，而是审核人尚不能访问同一份本地原始字节快照，
因此不能独立复算原始文件、HTTP 取证文件、本地 snapshot manifest 和执行人核验报告的
SHA-256。

本任务只处理该阻塞项：提供不进入 Git 的只读审核包，并要求独立审核人复算哈希与核对内容。

## 2. 非目标

- 不修改 `configs/g0/universe_time_boundaries.v1.json`；
- 不设置 `membership_evidence.status=verified`；
- 不设置 `g0_review.status=approved`；
- 不设置 `operational_status=eligible_for_d0`；
- 不运行 D0；
- 不把原始字节、压缩包或解析成分表提交到 Git；
- 不用所有者口头授权替代独立哈希复核。

## 3. 交付包

```text
handoff_manifest: manifests/review_handoffs/G0-T02_csindex_000906_20260703T100909Z.json
local_package: data/external/review_handoff/task=G0-T02/package=CSINDEX_000906_20260703T100909Z/G0-T02_CSINDEX_000906_20260703T100909Z.zip
package_sha256: fab0950153e3a683e9590dc895533276e0092146d7774b4822442d00d45b369b
contents_manifest_sha256: 823d6d3ffe75730fb5789170d6539f65477b458c5cf70f9389af716f18c3412d
```

交付包当前只存在于本地 `data/external/` 下，并受 `.gitignore` 排除。审核人必须通过
授权的本地工作区访问，或通过另行批准的非 Git 只读渠道取得同一包。

## 4. 独立审核要求

审核人必须与 G0-T01 证据获取执行人相互独立，并至少完成：

1. 复算交付包 SHA-256；
2. 解包并复算所有成员文件 SHA-256；
3. 核对成员文件与 `source_snapshot_manifest.json`、tracked manifest 和执行人核验报告一致；
4. 核对保存的 HTTP headers 与重定向链；
5. 打开 `000906cons.xls` 原始文件，独立确认指数代码、指数名称、成分数量、重复项、缺失项和文件日期；
6. 核对公告 `3006137`、调样附件和 000906 编制方案能否支持 `CSI800_STATIC_2026_06` 的精确生效日；
7. 在固定 commit 上记录审核结论、审核人、审核时间、独立性声明和是否批准 G0。

## 5. 当前结论

截至本任务创建时：

```text
membership_evidence.status: pending
g0_review.status: pending
operational_status: blocked_pending_official_membership_evidence
eligible_for_d0: false
```

只有在独立审核明确通过后，后续提交才可讨论将上述字段更新为 verified / approved /
eligible_for_d0。

## 6. 失败与回退

若审核人无法访问交付包、任一 SHA-256 不一致、内容与 manifest 不一致、独立性不满足，
或语义证据链不足以支持 `CSI800_STATIC_2026_06`，则 G0 继续保持 blocked，并不得进入 D0。
