# R1-T10 R1 验收门禁与 R2 交接矩阵

本任务是 `stage_acceptance_gate_and_handoff`，只消费已提交并通过 repository final gate 的上游证据。它不读取 MarketDB，不重新执行上游实验，不选择 winner，也不冻结参数。

正式矩阵固定为 12 行：4 行 shared-q 主线、4 行 T14 center、4 行 immediate-neighbor sidecar。判定严格按 `blocked_return_to_R0 → do_not_freeze → review_candidate → freeze_candidate` 优先级执行，不构造综合分数。

Author-draft 必须停在外部审阅前：`scientific_review_status=pending`、`repository_final_gate_status=pending`、`formal_task_completed=false`、`R2_allowed_to_start=false`。只有独立科学审阅、final-gate validator、Quality 和合并全部通过后，R2-T01 才能启动。
