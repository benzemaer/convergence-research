# R3-T01 研究协议、T0 与分析单位冻结

## 任务状态

```text
task_id: R3-T01
task_title: 研究协议、T0 与分析单位冻结
workflow_mode: same_pr
phase: implementation_review
implementation_review_status: pending
reviewed_implementation_sha:
formal_run_allowed: false
formal_run_status: not_started
formal_execution_sha:
run_id:
result_review_status: not_started
next_task_allowed: false
R3-T02_allowed_to_start: false
readme_advanced: false
formal_run_executed: false
```

本任务只提交 Implementation。它冻结 R3 的研究协议、实时 T0 锚点、全量
exit-attempt 主分析单位、component/attempt 身份、事件生命周期上下文、T0/T1/T2
基础时点、H5/H10/H20/H30 horizon、样本分割、purge/embargo 与相关性治理。
本任务不生成真实 exit attempt、真实 T0 数量、未来路径、收益、经验边界、五类
path label 或 T0/T1/T2 实际特征。

## 1. R2-T08 startup reconciliation

R3-T01 分支基于包含 R2-T08 PR #100 和 GOV-T02 PR #102 的 `main`。实现和未来
formal runner 都以 committed Git blobs 为 lineage source，不使用工作树内容替代
正式绑定。

```text
R2-T08 merge commit: 0cebf836302b3e89d5d8059c6992e154eea46610
GOV-T02 merge commit: e49d6cd66b46a54033e7b875bdaa4c58cc706b78
R2-T08 reviewed head: 90b3b25a5294a8cf9cab622de1f96c99ff3f29f6
R2-T08 scientific review: PR #100 review/comment 4691383571
R2-T08 authoritative run: R2-T08-20260714T055034Z
R2-T08 artifact commit: d7c01124620fc29067bf11504f8c31848bf26f08
R2-T08 committed-artifact validation commit: 58ad083e34c141ba97701e9760d71092a3a34bea
```

重新计算的三个 committed-byte SHA-256 必须为：

```text
r2_t08_r3_handoff_manifest.json = 6cefa5c7bcc97a0d64cc0d1febd27c25f84ab69a89b74ccf238fd36e9cb83ba1
r2_t08_release_anchor_obligation.json = c3afe1a6e25c571657677f8325dea403cdf1c808301a47ba18cb88012685d7c1
r2_t08_final_acceptance.json = 907831632fe449e50491135c49b7b1db0a795869496bb8d09d7506a6772aa516
```

启动还必须确认 PR #100 已合并、reviewed head 是 merge commit 的 ancestor、review
记录含 `scientific_review_status=passed`、`unique_r3_entrypoint=true`、
`alternative_entrypoints=[]`、`r3_handoff_eligible=true`，以及 committed-artifact
validation `status=passed`、`failure_count=0`。任一项失败都返回
`startup_status=blocked_return_to_R2`，不得生成 R3 contract。

R3-T01 只消费 PR #100 的 legacy R2 evidence；不复制 GOV-T01 的 author package、
SCIENTIFIC PASS、repository final gate 或 post-merge handoff 机制。R2-T08 的
author-stage `R3_allowed_to_start=false` 只有在 PR #100 merged direct binding 后才
激活 R3 启动资格；这不等于 T01 已完成。

## 2. 冻结输入与访问边界

唯一允许登记的 state versions 为：

```text
r2_s_pct_W120_K3_qP20_qC20_qT25_qV20_d2_g1_v8
r2_s_pcvt_W120_K3_qP20_qC20_qT20_qV30_d2_g1_v8
```

两条状态线均固定 `W=120`、`K=3`、`d=2`、`g=1`。strict core 只作为版本内部
字段，不能升级为独立 state version。W250、shared-q 独立版本、PCT parent、其他
版本和跨 state-version event merge 均被拒绝。

R2 canonical database 只登记，不打开：

```text
data/generated/r2/r2_t05/R2-T05-20260713T154957Z/r2_t05_canonical_materialization.duckdb
sha256=4488f0cca26f703890dfea8701ed761e7de124467a6d373f2e0264dbd0215129
```

T01 只冻结未来如何消费 `r2_canonical_daily_state`、`r2_canonical_event_zone` 和
`r2_canonical_event_membership`；不查询本地数据库，也不以 T06 replay database、
T03 source table 或同名文件替代 T05 canonical interfaces。三张 public interface 的
字段、primary key、row count、stable multiset hash 与 source run 必须逐项匹配 R2-T08
committed canonical-interface binding：
`data/generated/r2/r2_t08/R2-T08-20260714T055034Z/r2_t08_canonical_interface_binding.json`，
其 committed-byte SHA-256 为
`2a20c59b219f74c09cf0389add175522666d2f8777c6fd7668e18a75184ebb1f`。T01 只读取
该 Git blob 做接口绑定，不打开或查询 T05 DuckDB。

## 3. T0 anchor decision

R3 采用 `natural_exit_attempt`，来源语义为 R2 的 `provisional_component_exit`，
但不再把后者称为最终 release。它表示：一个已经取得 event 资格的事件中，本次
confirmed run 发生合法 natural exit 的日频 causal observation anchor。当前 R2
公共 daily 接口没有权威的日内 available-time 字段，因此 T01 只冻结交易日级 T0。

```text
selected_anchor: natural_exit_attempt
source_r2_candidate: provisional_component_exit
interpretation: real-time observable exit attempt anchor, not final event-zone exit
finalized_zone: rejected_as_T0_due_to_signal_delay_and_event_level_semantics
retrospective_final_exit: rejected_as_T0_due_to_lookahead
event_level_last_exit_observation_time: audit_and_reconciliation_only,
  never_used_to_enumerate_attempts
```

后续 reentry、same-zone requalification、merge、g 超限或 event finalization 都不能
追溯删除已经发生的 T0。`zone_finalization_time`、`last_exit_observation_time` 和
`last_confirmed_end_date` 只能用于事后审计或 reconciliation。

## 4. 合法 natural exit predicate

未来物化必须先在完整 expected-row surface 上按
`state_version_id × security_id × trade_date` 排序计算直接相邻的 `LAG`，然后再做
候选过滤。禁止先删除 invalid、ineligible、missing 或 unknown rows，再寻找上一条
有效行。

设直接前一 expected row 为 `p`、当前为 `c`。合法 T0 必须同时满足：

```text
p.eligible_state=true
p.quality_state=valid
p.confirmed_state=true
p.active_event_id_as_of IS NOT NULL
p.event_status_as_of IN (QUALIFIED_ACTIVE, REENTRY_PENDING_QUALIFICATION)
c expected row 实际存在
c.eligible_state=true
c.quality_state=valid
c.raw_state=false
c.confirmed_state=false
p 与 c 是完整 expected-row surface 上的直接相邻行
```

之后以 `event_id=p.active_event_id_as_of` 连接 event zone，确认 state/security 相同、
`first_qualification_time` 的交易日期不晚于 `c.trade_date`。当前 event ID 若非空但
不同于 prior ID，必须报 `EVENT_ID_CONFLICT` 等价的 contract failure，不能任选一个
ID。current membership row 可以缺失；它只用于 lifecycle audit，不决定 T0 是否存在。

```text
exit_attempt_date = c.trade_date
exit_attempt_time = NULL
exit_attempt_time_missing_reason = UPSTREAM_DAILY_AVAILABLE_TIME_NOT_EXPOSED
time_resolution = daily_causal_date_only
```

不得用 `membership_available_time`、`last_exit_observation_time`、
`zone_finalization_time`、`last_confirmed_end_date` 或人为构造的收盘时间替代
`exit_attempt_time`。若未来需要日内 available-time 语义，必须由独立上游接口任务
提供权威字段，不能由 R3 从 retrospective membership availability 推断。

`natural_state_exit` 必须通过 R2 transition registry 重放得到：

```text
CONFIRMED_ACTIVE → CONFIRMED_EXITED
trigger=raw_false, hard_break=false, reason_code=natural_state_exit
```

事件未取得资格、普通 prequalification raw=false、prior 未 confirmed、prior
invalid/ineligible/missing、current missing/ineligible、quality interruption、raw
unknown、unknown、blocked、diagnostic_required、expected_empty、right/sample-end
censoring、仅由 finalization 推断历史 exit、跨 invalid row 跳跃和跨 event/state
version 继承均不得生成正常 T0。invalid row 后重新出现 valid raw=false 也不得补造。

## 5. 主分析单位与身份

正式主分析单位是：

```text
state_version_id × event_id × exit_attempt_id
all_legal_exit_attempts_are_primary=true
first_attempt_only=false
later_attempts_are_sidecar=false
```

同一 event 内重新 confirmed 并再次 natural exit 时生成新的 attempt；重新达到 d
qualification 和尚未达到 d 的 unqualified reentry 都保留。后者不增加 qualified
component count，但必须保留 `unqualified_reentry=true`。

R2 public tables 没有原始 confirmed-run ID，T01 使用 namespace
`r3_causal_confirmed_run_v2`。primary component 只能由 complete expected-row surface
上的 causal daily rows 与 event-zone first-component metadata 重建：
`expected_row_present=true`、`eligible_state=true`、`quality_state=valid`、
`confirmed_state=true` 且 `active_event_id_as_of=event_id` 的直接连续行构成 causal
run；membership rows 只保留为 audit surface，不能创建、改写或排序 primary component。
首个 component 使用 event-zone `first_component_start_date` 与
`date(first_qualification_time)`；后续 component 使用该直接 causal run 内首个
`component_qualified_as_of=true` 的 daily trade date。其 ID 是对以下 canonical JSON
做完整 SHA-256：

```text
namespace, contract_version, state_version_id, event_id, security_id,
source_component_start_date
```

`source_component_ordinal` 按 `source_component_start_date, source_component_id` 排序，
在 `state_version_id × event_id` 内从 1 开始。`component_count_as_of_exit` 是截至
T0 date 已取得 causal qualification 的 distinct component 数；因此 unqualified
reentry 可以使 source ordinal 大于该 count。`frozen_g` 从冻结 state version 精确
映射。`last_observed_zone_revision_before_exit`、`current_exit_membership_zone_revision`
以及 current membership availability 均为 post-event audit 字段，不能成为 T0
特征或 primary identity。

Field Semantics Registry 已登记实际 attempt 输出字段：
`source_component_start_date`、`source_component_end_date`、
`source_component_qualification_date`、`source_component_qualified`、
`source_component_ordinal`、`component_count_as_of_exit`、`unqualified_reentry` 与
`attempt_weight`，并对全部 actual attempt output fields 执行覆盖检查。
`source_component_qualified` 由 causal daily state 与 event-zone metadata 派生，
不由 membership ledger 派生。

`exit_attempt_id` 使用 namespace `r3_exit_attempt_v3`，canonical 字段为
`namespace、contract_version、state_version_id、event_id、security_id、
source_component_id、exit_attempt_date`。不得加入 `exit_attempt_time`、finalization、
final count、final revision、resolution、future path/label 或 boundary version。
attempt ordinal 在 `state_version_id × event_id` 内按 `exit_attempt_date、ID` 排序。
重复 ID、重复 source-component/T0、ordinal 不连续、时间排序不一致或同一 T0 被多个
event 认领时必须 fail closed。

## 6. 字段、landmark 与 horizon

field-semantics registry 对每个字段登记 type、nullable、availability class、available
time source、T0/T1/T2 allowed、audit-only、forbidden-model-feature、source artifact
和 derivation rule。state/event/security/attempt/component identity、ordinal、
`component_count_as_of_exit`、T0 date/time、prior/current state、exit reason、g 与
event status 是 T0 可知字段。

`final_component_count`、最终 attempt 数、`event_balanced_weight`、resolution、
same-zone requalification、g-exceeded、future overlap final result、future path class
和 future boundary hit 都是 post-event/post-path 字段，不得作为 T0/T1/T2 特征。

```text
T0 = exit_attempt_date；exit_attempt_time = NULL（UPSTREAM_DAILY_AVAILABLE_TIME_NOT_EXPOSED）
T1 = T0 后第 1 个有效、可观测 expected trading row
T2 = T0 后第 2 个有效、可观测 expected trading row
```

有效行要求 expected row 存在、`eligible_state=true`、`quality_state=valid`。异常行
不计入 T1/T2 ordinal，但必须记录 intervening count、reason set 和 unavailable
reason，不得静默跳过。Horizon `H ∈ {5,10,20,30}` 从 T0 后第一个有效未来交易日
开始计数，T0 不计入：

```text
H5=early reaction
H10=short-term path formation
H20=primary path-classification horizon
H30=extension and late-reversal check
primary_horizon=H20
```

T01 不生成 PCVT、价格、成交量、环境、return、MFE、MAE、boundary hit、path class，
也不决定 R4 deferred prediction rule；不得用 calendar-day 替代有效交易日。

## 7. sample split 与相关性治理

```text
sample_split_unit=event_id
all_attempts_of_same_event_in_same_split=true
attempt_level_random_split=forbidden
split_roles=design | validation | out_of_sample | purged
purge_embargo_length=32 valid trading days
purge_basis=H30 plus maximum T2 landmark delay
split_topology_status=frozen
calendar_boundary_status=unselected_by_design
calendar_boundary_selection_owner=R3-T02
cluster_key_primary=event_id
cluster_key_robustness=security_id
future_window_overlap_marker_required=true
attempt_weight=1.0
event_balanced_weight=1 / final attempt count within event
```

同一 event 的所有 attempts 必须进入同一 split。允许用于日期边界选择的信息只有
date/event/attempt/observability coverage；future labels、returns、boundary hits、
model accuracy 和 trading results 禁止。R3-T02 必须在 control matching、standardization
或 empirical-boundary estimation 之前登记并冻结具体日期边界。

## 8. Implementation、合成测试与门禁

核心实现位于 `src/r3/r3_t01_protocol.py` 和独立的
`src/r3/r3_t01_validator.py`；`scripts/r3` 只处理参数、路径、日志和退出码。配置、
五个 Draft 2020-12 schema、21 个可执行合成场景和 24 个 mutation 位于本任务列明路径。
原 S04–S11、S13、S22 的零行或文本-only 场景已从 formal fixture 移除，相关
lifecycle、split 和 attempt-registry 约束改由真实 unit/contract test 覆盖；S12 已
改为包含 T0/T1/T2 与 H5/H10/H20/H30 实际输出的可执行 Horizon 正例。
validator 直接读取实际 runner artifacts、config、schema、committed upstream bindings
和 synthetic fixture，独立重放 transition、独立生成 ID/ordinal，不导入 production
decision helper；result analyzer 在 validator passed 后重新读取实际 artifacts，并在
最终 manifest 中记录实际 artifact owner/kind、hash、size、row count、适用 schema
绑定，以及 formal config/fixture 的 committed Git source binding。analyzer 只能写出
`analysis_complete_pending_final_validation`，不能自行写 `completed`；terminal
validator 在 manifest 之后只写 `r3_t01_final_validation.json` sidecar，并绑定
manifest SHA，且不得修改既有 artifact。

当前 formal fixture 的 S01–S03、S12、S14–S21、S23–S31 覆盖 first exit、
requalified/unqualified reentry、state/security namespace、ordinal/count divergence、
T0/T1/T2、invalid-row audit、horizon counting、prequalification、quality/ineligible/
unknown、prior not confirmed、filtered-row lag、censoring、ID conflict、optional
membership 和 cross-group landmark isolation。duplicate identity/ordinal 通过独立
attempt-registry unit test 覆盖。每个 mutation 从磁盘
重新加载 pristine inputs，并经完整 validator 入口检查具体 error code：

```text
M01 FROZEN_STATE_VERSION_MISMATCH
M02 FROZEN_STATE_VERSION_SET_MISMATCH
M03/M04 ANCHOR_DECISION_MISMATCH
M05 FIRST_ATTEMPT_ONLY_FORBIDDEN
M06 LAG_AFTER_FILTERING_FORBIDDEN
M07 LATER_ATTEMPT_RISK_SET_POLICY_MISMATCH
M08 POST_EVENT_FIELD_IN_ID
M09 CALENDAR_DAY_HORIZON_FORBIDDEN
M10 ATTEMPT_LEVEL_RANDOM_SPLIT_FORBIDDEN
M11 EVENT_SPLIT_LEAKAGE
M12 DOWNSTREAM_GATE_OPEN
M13 FUTURE_OUTCOME_IN_T01
M14 VALIDATOR_PRODUCTION_HELPER_REUSE
M15 MEMBERSHIP_AVAILABILITY_NOT_T0
M16 NON_PUBLIC_CANONICAL_FIELD_REFERENCE
M17 LANDMARK_CROSS_GROUP_CONTAMINATION
M18 EMPTY_MUTATION_RESULTS
M19 PENDING_FORMAL_ARTIFACT
M20 NON_AUTHORITATIVE_TIME_IN_ID
M21 MEMBERSHIP_LOOKAHEAD_LEAK
M22 MEMBERSHIP_DERIVED_PRIMARY_ID_FORBIDDEN
M23 CANONICAL_INTERFACE_PRIMARY_KEY_MISMATCH
M24 FINAL_VALIDATION_TAMPER_DETECTED
```

Implementation checks：

```text
python -m compileall -q src scripts tests
python -m ruff format --check src scripts tests
python -m ruff check src scripts tests
python scripts/validate_configs.py
python scripts/validate_manifests.py
python scripts/validate_text_contract.py
python scripts/build_compendium.py --check
python -m unittest discover -s tests/r3 -p "test_r3_t01*.py" -v
python -m unittest tests.test_task_index_current -v
python scripts/r3/final_validate_r3_t01_protocol.py --run-dir <RUN_DIR>
git diff --check
```

## 9. 用户批准后的 formal-run runbook

用户明确批准实际 implementation SHA 后，才允许：

```text
python scripts/r3/run_r3_t01_protocol.py \
  --config configs/r3/r3_t01_protocol_t0_analysis_unit.v1.json \
  --reviewed-implementation-sha <APPROVED_40_SHA> \
  --approval-comment-id <APPROVAL_COMMENT_ID>

python scripts/r3/validate_r3_t01_protocol.py \
  --run-dir data/generated/r3/r3_t01/<RUN_ID>

python scripts/r3/analyze_r3_t01_results.py \
  --run-dir data/generated/r3/r3_t01/<RUN_ID> \
  --config configs/r3/r3_t01_protocol_t0_analysis_unit.v1.json \
  --fixture tests/r3/fixtures/r3_t01/cases.json \
  --reviewed-implementation-sha <APPROVED_40_SHA> \
  --formal-execution-sha <APPROVED_40_SHA>

python scripts/r3/final_validate_r3_t01_protocol.py \
  --run-dir data/generated/r3/r3_t01/<RUN_ID>
```

formal runner 要求 GitHub PR #103 保持 OPEN、HEAD 等于批准 SHA、工作树 clean、
approval comment 来自 `benzemaer` 且逐行唯一包含
`task_id=R3-T01`、`implementation_review_status=approved`、
`reviewed_implementation_sha=<APPROVED_40_SHA>`、`formal_run_allowed=true` 和
`approval_scope=R3-T01_formal_run_only`，并且 R2-T08 startup binding 通过，
并且只读取 committed R2/R3 protocol artifacts 与 synthetic fixtures；不得打开 T05
canonical DuckDB。未来产物至少包含 upstream binding、anchor、protocol/transition/
analysis/field/landmark/split/schema contracts、synthetic/mutation results、anomaly
scan、manifest、validator result 和独立 result analysis。runner 首次只报告
`formal_generation_status=generated_pending_validation`，不写 validator、mutation、
analysis 或 manifest 占位文件。正式运行后必须依次读取实际 artifacts，复核 hashes、
ancestry、每个 case 的 expected/actual attempts、ID/ordinal、排除、reentry、T1/T2/
horizon、field availability、split、mutation 和 double rebuild，再由 result analyzer
生成最终 manifest，最后执行 terminal validator；validator 通过不等于科学结果通过。

本轮未执行 formal run，未创建 `data/generated/r3/r3_t01/<RUN_ID>/`，未打开或查询
canonical DuckDB，未枚举真实 exit attempts，未生成真实 T0 counts、future return、
boundary、label、model output 或正式 result analysis。README 保持
`R3-T01_status=implementation_review`、`formal_run_allowed=false`、
`formal_run_status=not_started`、`result_review_status=not_started`、
`readme_advanced=false`、`R3-T02_allowed_to_start=false`。

## 10. 回退

改变 T0 anchor、attempt enumeration、identity/ordinal、landmark、horizon 或 split
语义时，必须创建新的 R3-T01 contract version，并重新审阅。改变 R2 state/version、
confirmed interval、d、g 或 event identity 时，最低退回 R2。R3-T02 只有在本
Implementation 被用户审阅接受并完成后续授权后才可开始。
