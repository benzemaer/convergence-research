# R0-T15 层级 q-vector 正式物化与 R1-T14-02 交接

`task_id`: R0-T15

## 目标

逐字消费 R1-T14-01 的 frozen materialization registry，使用既有 R0 strict-past score、dimension/nested/confirmation/interval 语义，正式物化全部 nonbaseline centers 与 immediate neighbors，并形成供 R1-T14-02 使用的 author-draft handoff candidate。

## Goal 内部 continuation 边界

本 task 的 stacked implementation 由 PR-A #87 的精确 head commit `2e2cc2931a4c3ff1ab427966bc78f79a0f69c151`、result-package hash、author-analysis hash 和 request hash 共同授权。`goal_internal_r0_materialization_authorized=true` 只允许构建 stacked Draft PR-B；`repository_r0_materialization_gate_passed=false`，不得据此更新 README 正式 gate。

## 输入与禁止事项

只允许读取 R1-T14-01 final author-draft request、R0-T05 strict-past dimension score 和既有 R0-T07 baseline artifacts。禁止重新计算 raw metrics、strict-past percentile score、候选选择或 archetype ranking；禁止新增 vector、删除不利 neighbor、修改 q/W/K 或按物化结果重新选择 center。

## 输出与验收

正式输出 dimension state、nested daily state、K=3 daily confirmation、confirmed intervals、candidate registry、artifact manifest、author-draft handoff manifest、baseline reconciliation、anomaly scan、result analysis、evidence、engineering validation 和 result package。baseline 只绑定既有 lineage，不重复复制。

内部 continuation 只有在 request registry 完全一致、baseline mismatch_count=0、parent-child/confirmation/interval conservation 通过、schema/hash/manifest 完整且 `blocking_findings=[]`、`unresolved_questions=[]` 时才可通过。首次 author-draft 必须保持 independent review 与 repository final gate pending，`R1-T14-02_allowed_to_start=false`、`formal_task_completed=false`。
