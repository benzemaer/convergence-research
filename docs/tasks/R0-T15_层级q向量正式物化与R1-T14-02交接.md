# R0-T15 层级 q-vector 正式物化与 R1-T14-02 交接

`task_id`: R0-T15

## 目标

逐字消费 R1-T14-01 的 frozen materialization registry，使用既有 R0 strict-past score、dimension/nested/confirmation/interval 语义，正式物化全部 nonbaseline centers 与 immediate neighbors，并形成供 R1-T14-02 使用的 author-draft handoff candidate。

## Goal 内部 continuation 与 REV1 边界

原 run `R0-T15-20260710T2136Z` 由 PR-A #87 author head `2e2cc2931a4c3ff1ab427966bc78f79a0f69c151` 的 goal-internal continuation 授权。该执行时 lineage 必须原样保留，不得事后改写为已消费 #87 final gate。PR #87 现已由 finalization commit `be446a8fe73a8575c76bee3ee1975993010cad93` 完成独立 scientific review，并通过 merge commit `13598861a620ab9e599957db3a34f8595630035b` 合入 main；REV1 只将这条 post-run final authorization 与原执行 lineage 并列绑定。

外部审阅评论 `4941872279` 判定旧 handoff 的 manifest/registry 文件哈希 stale。REV1 只修复文本归一化与交接 lineage，不重新物化、不覆盖四张 DuckDB、不修改 request、registry、artifact manifest、execution summary 或原 execution config。旧 handoff/package/analysis/evidence 必须按原字节归档，新 canonical handoff/package 必须保持 `pending_rereview`，且 goal-internal、repository 与 formal T14-02 gate 全部关闭。

## 输入与禁止事项

原执行只允许读取 R1-T14-01 author-draft request、R0-T05 strict-past dimension score 和既有 R0-T07 baseline artifacts。REV1 只允许读取原执行产物、#87 final result package/scientific review/final-gate record 与本地 DuckDB attestation。禁止重新计算 raw metrics、strict-past percentile score、候选选择或 archetype ranking；禁止新增 vector、删除不利 neighbor、修改 q/W/K 或按物化结果重新选择 center。

## 输出与验收

原正式输出为 dimension state、nested daily state、K=3 daily confirmation、confirmed intervals、candidate registry、artifact manifest、author-draft handoff manifest、baseline reconciliation、anomaly scan、result analysis、evidence、engineering validation 和 result package。baseline 只绑定既有 lineage，不重复复制。REV1 新增 author revision record、原 package/handoff/analysis/evidence 字节归档、本地 DuckDB attestation、修订后的 analysis/evidence、handoff 与 result package；四库继续 local-only，不提交或上传。

REV1 validator 必须重新读取四库字节并复算 SHA、row/vector count、主键、raw/confirmed parent-child 与 interval duration conservation，同时交叉验证 package↔handoff↔manifest↔registry、两个 archive 和 #87 final gate。该 attestation 是 implementation-side local reread，不是外部 reviewer 的直接字节复核。

外部复审评论 `4943245857` 已在 reviewed HEAD `3210c35a6a5a5679792bfd455969e78664fc5e13` 上对 REV1 给出 PASS。Repository final-gate commit 必须保存被复审 package/analysis/evidence 的原字节，保持 canonical handoff 不变，并 fail-closed 绑定复审记录、reviewed REV1 archives、author-revision validator、manifest/registry 与本地 attestation。#88 merge 发生前必须保持 `goal_internal_continuation_allowed=false`、`R1-T14-02_allowed_to_start=false`、`R1-T10_allowed_to_start=false`、`R2_allowed_to_start=false`、`formal_task_completed=false` 与 `selection_path_not_independently_confirmed=true`；只有 #88 合并后，下游才能另行绑定 merge commit 并授权新的 R1-T14-02 run。
